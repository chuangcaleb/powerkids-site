/**
 * Placeholder home page.
 *
 * Replaced in Phase 4 by the catch-all route that resolves a `pages` record by
 * slug and renders its blocks. It exists now only so the build has an entry
 * point and the deploy pipeline can be proven end to end.
 */
export default function HomePage() {
  return (
    <main>
      <h1>PowerKids Kindergarten</h1>
      <p>Rebuild in progress.</p>
    </main>
  )
}
