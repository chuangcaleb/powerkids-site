---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";
interface Props extends HTMLAttributes<"section"> {
  contentWidth?: "full" | "lg" | "xl"; // default: max-w-xl
  // paddingX?: "always" | "none" | "mobile"; // default: (gutters only on) mobile
  noPaddingY?: boolean; // default: paddingY true
  // variant?: "base" | "dark" | "secondary"; // default: base
}
const {
  contentWidth = "lg",
  // paddingX = "mobile",
  noPaddingY,
  // variant = "base",
  class: extraClass,
  ...attrs
} = Astro.props;
---

<section
  class={cn(
    "fl-px-xs md:fl-px-m",
    contentWidth === "lg" && "max-w-screen-lg mx-auto",
    contentWidth === "xl" && "max-w-screen-xl mx-auto",
    !noPaddingY && "fl-py-2xl first-of-type:fl-pt-l",
    extraClass,
  )}
  {...attrs}
>
  <slot />
</section>
